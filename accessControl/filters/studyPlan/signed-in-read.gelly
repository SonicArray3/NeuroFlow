# TODO: Gadget assistant picked the "userId" field for tenancy but couldn't locate a field with that name on model studyPlan -- this needs to be fixed!
filter ($session: Session, $user: User) on StudyPlan [
  where true == false
]
# structured filters: [{"type":"tenancy_read_filter","field":"userId"}]